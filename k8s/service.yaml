apiVersion: v1
kind: Service
metadata:
  name: mi-microservicio-service
spec:
  selector:
    app: mi-microservicio
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: NodePort
